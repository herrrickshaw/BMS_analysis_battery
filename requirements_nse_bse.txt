# NSE/BSE Stock Data Extractor — Python dependencies
# Install: pip install -r requirements_nse_bse.txt

# Core data
pandas>=2.0.0
numpy>=1.24.0

# NSE data (primary source for NSE-specific fields)
nsepython>=2.9

# Yahoo Finance (fundamentals, price history, BSE via .BO suffix)
yfinance>=0.2.36

# HTTP + parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Progress + logging
tqdm>=4.66.0

# Optional: faster CSV I/O
pyarrow>=14.0.0
